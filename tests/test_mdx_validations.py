import pytest
pytest.skip("Moved to skill scripts (.github/skills/blog-post-creator/scripts). Run them manually if needed.", allow_module_level=True)

# If you need to run the validation from tests, invoke the script from the skill folder:
# python .github/skills/blog-post-creator/scripts/validate_mdx.py
